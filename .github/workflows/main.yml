name:  Echo

on:
  workflow_dispatch:

  watch:
    types: [started]
jobs:

  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print the input tag to STDOUT
        run: echo  The tags are 
